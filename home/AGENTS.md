# AGENTS.md

If the user asks to make changes to this system, note that there is a Nix configuration at `.config/nix-darwin` — the system is managed declaratively from there, not by editing files by hand.
